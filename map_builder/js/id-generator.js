import {getState} from './state.js';export function nextId(key){const base={regions:'region',vegetation:'tree',structures:'structure',walls:'wall',actors:'actor',spawn_points:'spawn',interactables:'interactable',pickups:'pickup',rivers:'river',roads:'road',bridges:'bridge',objectives:'objective',encounters:'encounter',doors:'door'}[key]||'entity';const used=new Set((getState()[key]||[]).map(x=>x.id));let n=1;while(used.has(`${base}_${n}`))n++;return `${base}_${n}`}
