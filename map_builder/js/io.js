import {validateMapSpec} from './validation.js';import {replaceState,getState} from './state.js';export function exportMap(show){const result=validateMapSpec(getState());show(result);if(result.errors.length)return;const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(getState(),null,2)],{type:'application/json'}));a.download=`${getState().map.id||'map'}.json`;a.click();URL.revokeObjectURL(a.href)}export function importMap(file,show){const r=new FileReader();r.onload=()=>{try{const data=JSON.parse(r.result),result=validateMapSpec(data);show(result);if(!result.errors.length)replaceState(data)}catch(e){show({errors:[{path:'/',message:`invalid JSON: ${e.message}`}],warnings:[]})}};r.readAsText(file)}
