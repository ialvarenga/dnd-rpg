import {getState,setPlayerField} from '../state.js';import {byType} from '../asset-catalog.js';export function playerPanel(){const p=getState().player||{};return `<fieldset><legend>Player (optional)</legend><label>Character asset<select data-player="character_asset"><option value="">— none —</option>${byType('character').map(a=>`<option value="${a.id}" ${a.id===p.character_asset?'selected':''}>${a.id}</option>`).join('')}</select></label></fieldset>`}export function bindPlayer(root){const e=root.querySelector('[data-player]');if(e)e.onchange=()=>setPlayerField('character_asset',e.value)}
