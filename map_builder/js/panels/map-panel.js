import {getState,setMapField} from '../state.js';import {ENUMS} from '../schema-constants.js';const input=(label,key,value,type='text')=>`<label>${label}<input data-map="${key}" type="${type}" value="${value??''}"></label>`;export function mapPanel(){const m=getState().map;return `<fieldset><legend>Map</legend>${input('Map ID','id',m.id)}<label>Preset<select data-map="preset">${ENUMS.preset.map(x=>`<option ${x===m.preset?'selected':''}>${x}</option>`).join('')}</select></label><div class="row">${input('Seed','seed',m.seed,'number')}${input('Width (m)','width_m',m.bounds.width_m,'number')}</div>${input('Height (m)','height_m',m.bounds.height_m,'number')}${input('Asset catalog version','asset_catalog_version',m.asset_catalog_version||'')}${input('Generator version','generator_version',m.generator_version||'')}</fieldset>`}export function bindMap(root){root.querySelectorAll('[data-map]').forEach(e=>e.onchange=()=>setMapField(e.dataset.map,e.type==='number'?Number(e.value):e.value))}
